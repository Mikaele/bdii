<h1 class="text-center">Aniversariantes do Mês </h1>

<%= link_to 'Novo Cliente', new_cliente_path, class:"btn" %>

<p></p>
<table class="table table-hover table-condensed">
  <thead>
  <tr>
    <th>DIA</th>
    <th>CLIENTE</th>
    <th>EMAIL</th>
    <th>SEXO</th>
    <th>OBS</th>
  </tr>
  </thead>

  <tbody>
<% @clientes.each do |cliente| %>
  <tr>
    <td><%= cliente.aniversario.strftime("%d") %></td>
    <td><%= cliente.pessoa.nome %></td>
    <td><%= cliente.pessoa.email %></td>
    <td><%= cliente.sexo %></td>
    <td><%= cliente.obs %></td>
  </tr>
<% end %>
</tbody>
</table>
